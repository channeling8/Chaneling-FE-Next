import { noIndexMetadata } from '@/config/metadata'

export const metadata = noIndexMetadata

export default function LandingReportLayout({ children }: { children: React.ReactNode }) {
    return children
}
